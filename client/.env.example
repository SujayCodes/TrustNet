# For local dev, leave this unset - Vite's dev proxy forwards /api to localhost:4000.
# For Vercel deployment, set this to your deployed backend's URL, e.g.:
# VITE_API_BASE=https://trustnet-api.onrender.com/api
VITE_API_BASE=
