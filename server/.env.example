PORT=4000
JWT_SECRET=change-this-to-a-long-random-string
# Comma-separated list of allowed frontend origins for CORS, e.g. your Vercel URL.
# Leave unset locally to allow all origins.
FRONTEND_URL=
# Optional: where to store the SQLite file. Defaults to ./data inside the server folder.
DATA_DIR=
